var navegador = {
    nombre: navigator.appName,
    idioma: navigator.language,
    version: navigator.appVersion,
    codigo: navigator.appCodeName,
    plataforma: navigator.platform,
    cookies: navigator.cookieEnabled,
    cabecera: navigator.userAgent
}